-- pg_eddy--0.3.0--0.4.0.sql
-- Migration from Phase 2 (v0.3.0) to Phase 3 (v0.4.0): MVCC and VACUUM.
--
-- New Rust functions added in this release (auto-generated by pgrx from
-- #[pg_extern] in lib.rs):
--   • pg_eddy.update_node(node_id BIGINT, labels TEXT[], properties JSONB) RETURNS BOOLEAN
--   • pg_eddy.delete_node(node_id BIGINT) RETURNS BOOLEAN
--   • pg_eddy.am_stats() RETURNS JSONB
--
-- The VACUUM callback (stub_relation_vacuum in am.rs) now calls
-- vacuum_relation() instead of raising an error, so VACUUM on the node/edge
-- tables is fully functional.
--
-- No schema objects need to be added/altered for this upgrade; all new
-- behaviour is implemented in the Rust layer.  The SQL functions are created
-- automatically by pgrx from the #[pg_extern] annotations.

-- (intentionally empty — pgrx generates the CREATE FUNCTION stmts)
